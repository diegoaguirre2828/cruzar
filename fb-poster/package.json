{
  "name": "cruzar-fb-poster",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --import tsx src/index.ts",
    "run-once": "RUN_ONCE=1 node --import tsx src/index.ts",
    "capture-session": "node --import tsx src/capture-session.ts",
    "comment-bot": "node --import tsx src/comment-bot.ts",
    "scrape-groups": "node --import tsx src/group-scraper.ts",
    "tiktok-upload": "node --import tsx src/tiktok-upload.ts",
    "tiktok-capture-session": "node --import tsx src/tiktok-capture-session.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.4.1",
    "playwright": "^1.47.0",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
